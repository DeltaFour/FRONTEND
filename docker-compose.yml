services:
  ponto-eletronico-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: gabrielfogo/ponto-eletronico-frontend:latest
    env_file:
      - .env
    ports:
      - "5173:8080"
    restart: always
