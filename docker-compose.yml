services:
  ponto-eletronico-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: gabrielfogo/ponto-eletronico-frontend:latest
    env_file:
      - .env
    ports:
      - "3000:80"
    restart: always
